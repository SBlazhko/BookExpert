<% if user_signed_in? && current_user.id == @user.id %>
		<% if current_user.role == "user" %>	
			<%= render 'users/user_profile' %>
		<% elsif current_user.role == "reviwer" %>
			<%= render 'users/reviwer_profile' %>
		<% end %>
<% end %>